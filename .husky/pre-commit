#!/usr/bin/env sh
. "$(dirname -- "$0")/_/husky.sh"

echo "🔍 Running pre-commit checks..."

# Run lint-staged with proper flags
pnpm lint-staged --concurrent false --relative

echo "✅ Pre-commit checks passed!"
